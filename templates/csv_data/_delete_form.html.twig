<form method="post" action="{{ path('csv_data_delete', {'id': csv_datum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ csv_datum.id) }}">
    <button class="btn">Delete</button>
</form>
