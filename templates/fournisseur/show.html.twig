{% extends 'base.html.twig' %}

{% block title %}Fournisseur{% endblock %}

{% block body %}
    <h1>Fournisseur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fournisseur.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ fournisseur.createdAt ? fournisseur.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ fournisseur.updatedAt ? fournisseur.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ fournisseur.name }}</td>
            </tr>
            <tr>
                <th>Dateborn</th>
                <td>{{ fournisseur.dateborn ? fournisseur.dateborn|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Lieunaissance</th>
                <td>{{ fournisseur.lieunaissance }}</td>
            </tr>
            <tr>
                <th>Sexe</th>
                <td>{{ fournisseur.sexe }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ fournisseur.phone }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ fournisseur.adresse }}</td>
            </tr>
            <tr>
                <th>ImageFilename</th>
                <td>{{ fournisseur.imageFilename }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fournisseur_index') }}">back to list</a>

    <a href="{{ path('fournisseur_edit', {'id': fournisseur.id}) }}">edit</a>

    {{ include('fournisseur/_delete_form.html.twig') }}
{% endblock %}
